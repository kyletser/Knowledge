# 公众号订阅自动化 · 项目交接文档

> 交接日期：2026-09-06　写给接手的 AI：**先读完第 8、9 节再动手**，那两节是踩过的坑和未决的架构选择，能帮你少走半天弯路。
> 配套阅读：`公众号订阅自动化方案.md`（方案审查与落地记录，偏设计论证）、`AI/.tools/sync.py` 文件头注释（偏用法）。

---

## 1. 一句话概述

一个**纯本地**的微信公众号文章归档管线：把文章链接喂进去 → 自动抓正文 + 图片本地化 + 剥推广 → 按格式规范落成 markdown → 重建阅读索引。产物全部落在 `AI/` 目录下，不依赖任何云服务即可运行。

仓库根目录：`C:\Users\Administrator\Desktop\Knowledge`

---

## 2. 快速开始

**解释器必须用这个 venv 的**（已装好 requests / beautifulsoup4 / markdownify）：

```
C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe
```

```bash
cd C:/Users/Administrator/Desktop/Knowledge

# 完整跑一轮（检测 + 去重 + 归档 + 重建索引）
C:/Users/Administrator/.workbuddy/binaries/python/envs/default/Scripts/python.exe AI/.tools/sync.py

# 只列出会归档什么，不真抓（排查问题首选）
... sync.py --dry-run

# 只重建阅读索引（改了 state 或手动补了文章后用它）
... sync.py --index-only

# 跳过 RSS 检测，只消费 inbox.txt
... sync.py --inbox-only
```

缺依赖时：`-m pip install -r AI/.tools/requirements-wechat.txt`

---

## 3. 文件地图

| 路径 | 作用 | 是否可改 |
|---|---|---|
| `AI/.tools/sync.py` | **主管线**。检测→去重→归档→索引，并补齐三条铁律 | 可改 |
| `AI/.tools/wechat_archive.py` | **原有抓取器**，未改动。抓正文、图片本地化、剥推广话术 | **建议不动** |
| `AI/.tools/subscriptions.json` | 订阅配置：全局 settings + detector 开关 + accounts 清单 | 常改 |
| `AI/.tools/archive_state.json` | **去重状态库**，含 `archived` / `skipped` 两个桶 | 少动 |
| `AI/.tools/inbox.txt` | **手动补链接通道**，`#` 开头为注释 | 常改 |
| `AI/.tools/docker-compose.wechat.yml` | we-mp-rss 编排（检测服务，当前未启用） | - |
| `AI/.tools/requirements-wechat.txt` | 依赖：beautifulsoup4 / markdownify / requests | - |
| `AI/.tools/wechat2md/` | **空目录**，历史遗留，无内容 | 可删 |
| `AI/*.md` | 归档正文 | 产物 |
| `AI/assets/<标题>/` | 该文的本地图片 | 产物 |
| `AI/阅读.html`、`AI/阅读索引.md` | 阅读索引（带搜索 + 按号筛选） | 产物 |

> 说明：`wechat_archive.py` 除正文外还会额外存一份 `original.html` 保真备份，这是有益的，别当成垃圾清理掉。

---

## 4. 管线执行链路

```
inbox.txt ──┐
             ├─→ 收集候选 ─→ 去重（比对 archive_state） ─→ 逐篇归档 ─→ 落盘 & 重建索引
RSS 检测 ───┘                                                  │
                                                                └─→ 状态回写 archive_state.json
```

**去重规则**：URL 命中 `state["archived"]` 或 `state["skipped"]` 或本轮已见 → 跳过。保证不漏不重。

**归档后的四种状态**（汇总输出里会出现）：

| 状态 | 含义 | 后续动作 |
|---|---|---|
| `ok` | 归档成功 | 从 inbox 移除，写入 archived |
| `exists` | 已存在 / 重复 | 写入 skipped |
| `zero_image` | 图片数低于门槛，**已删掉已下载产物** | 写入 skipped |
| `rate_limited` | 微信限流/验证码 | **保留在 inbox，下轮重试，本轮不重试** |
| `failed` | 其他失败 | 保留在 inbox，下轮重试 |

---

## 5. 配置参考

`subscriptions.json` 当前实际内容（2026-09-06 实测）：

```json
{
  "settings": {
    "output_dir": "AI",
    "soft_wrap_width": 200,
    "min_images": 1,
    "fetch_timeout": 30,
    "检测范围_天": 3          // ⚠️ 死配置，代码从不读取，见第 9 节
  },
  "detector": {
    "enabled": false,
    "base_url": "http://localhost:8001"   // ⚠️ 死配置，代码从不读取，见第 9 节
  },
  "accounts": [
    { "name": "腾讯程序员", "enabled": true, "rss": "" },
    { "name": "阿里技术",   "enabled": true, "rss": "" }
  ]
}
```

**真正生效的字段只有四个**：`settings.min_images`、`settings.soft_wrap_width`、`settings.fetch_timeout`、`settings.output_dir`，加上 `detector.enabled` 和每个 account 的 `enabled` / `rss`。

- `rss` 填**完整 URL**（如 `http://localhost:8001/feed/xxxx.xml`），代码直接 `requests.get(rss)`，不做任何拼接。
- `rss` 留空 = 该号只走 inbox 手动通道。
- `detector.enabled=false`（当前）→ 完全不碰 RSS，只消费 inbox。

---

## 6. 三条归档铁律（Kyle 明确要求的规范，别破坏）

1. **软断行 ≤200 字符**——只断正文段落，跳过代码块、图片、标题、表格、列表。
   ⚠️ 引用行（`>` 开头）不整体跳过，而是**保留 `>` 前缀、只断行内容**。早期版本整体跳过引用行，结果微信的长导语导致仍有 282 字符超标行。修复后实测：最长行 308 → 200，超标行数 5 → 0，图片零损失。
2. **零图不保存**——正文图片数 < `min_images`（默认 1）则放弃保存，并**删掉已下载的 md + assets + original.html**，不留垃圾。
3. **限流不重试**——命中「访问过于频繁 / 环境异常 / 验证码」即止，单次请求，链接留 inbox 等下一轮。绝不反复请求加重风控。

**归档正文格式**（`wechat_archive.py` 产出）：

```markdown
# 标题

> 公众号：XXX
> 原文链接：[url](url)

---

![](./assets/<标题>/<hash>.png)
```

---

## 7. 定时任务

| 项 | 值 |
|---|---|
| 名称 | 公众号订阅每日同步 |
| ID | `512ae303-2949-4906-bf0b-848085a921b7` |
| 状态 | ACTIVE |
| 频率 | 每日 07:30 |
| 执行记录 | `.workbuddy/memory/automations/512ae303-2949-4906-bf0b-848085a921b7/memory.md` |

任务内容是调用 sync.py 并汇总回报。若电脑 07:30 未开机则当天跳过，状态库保证下次补上、不漏不重。

---

## 8. 当前状态（2026-09-06 实测）

| 项 | 值 |
|---|---|
| 已归档 | **16 篇** |
| skipped | 0 |
| 按号分布 | 腾讯程序员 8、阿里技术 1、图灵Agent评测 1、腾讯技术工程 1、行亦 1、陈以强 1、王僖 1、聂晓龙 1、Anne 1 |
| 订阅清单 | 腾讯程序员、阿里技术（**两者 rss 均为空**） |
| detector.enabled | **false** |
| Docker 守护进程 | **未运行**（`docker info` 连不上 npipe） |
| 自动检测通道 | **完全未接通** → 每轮自动跑必然 0 新增，只有 inbox 投链接才会归档 |

---

## 9. 五个坑（必读，都是踩过的）

### 坑 1：`detector.base_url` 是死配置
`discover_from_rss()` 直接用每个账号 `rss` 字段的**完整 URL** 去 `requests.get`，`base_url` 从头到尾没被读取过。**改 `base_url` 不会有任何效果**。要接通自动检测，唯一办法是给每个 account 填真实 RSS 地址 + `detector.enabled=true`。

### 坑 2：`settings.检测范围_天` 也是死配置，且 RSS 无日期过滤
该字段只在默认设置里定义（sync.py:49），代码中**从不读取**。`discover_from_rss()` 拿到 feed 后只过滤「链接含 mp.weixin.qq.com」，**不按发布日期过滤**。
⚠️ 后果：一旦给账号填上真实 RSS 并开启 detector，**首轮会把 feed 里所有历史文章全部尝试归档**，可能是几十上百篇、并容易触发微信限流。接手后若要开启自动检测，**建议先 `--dry-run` 看规模**，必要时给 `discover_from_rss` 补日期过滤。

### 坑 3：走 inbox 归档的文章，来源会被记成 `inbox`
`sync.py:442` 写的是 `"account": acc or "inbox"`，而 inbox 通道传的 `acc` 恒为空 → state 里记成 `inbox`。但 `wechat_archive.py` 会从正文提取**真实公众号名**写进 md 的 blockquote。
→ 结果：md 里是「阿里技术」，state 和阅读索引筛选器里是「inbox」，两者不一致。
**修法**：手工把 `archive_state.json` 该条的 `account` 改成真实号名，再 `--index-only` 重建索引。（2026-09-06 归档阿里技术那篇时已这样处理过一次。）

### 坑 4：两套系统互不连通（最容易误解，见第 10 节）
Kyle 曾扫过码，但那次授权喂的是**个人站 peronalsite**，与本管线无关。别默认"扫过码就等于这里能自动拉"。

### 坑 5：图片字节冗余
部分公众号的通用装饰图/动图会在该号**每篇文章里各存一份**，`AI/` 目录因此偏大（此前实测约 69MB）。这是「按文章隔离资源」的结构性代价，消除需改共享图池 + 批量改链接，性价比低，**暂不建议动**。

---

## 10. 两套系统的关系（关键背景）

这是最容易让接手者困惑的地方，务必理解：

| | 本地管线（本仓库） | 个人站 peronalsite |
|---|---|---|
| 位置 | `C:\...\Knowledge\AI\.tools\sync.py` | 服务器 `150.158.89.70` |
| 授权方式 | 无（直接抓公开文章） | **微信读书 Cookie**（Kyle 此前扫码的那个） |
| 产物 | `AI/*.md` + 本地图片 + 阅读索引 | 站内数据库 + 站内阅读器 |
| 当前可用性 | ✅ 可用（走 inbox） | ✅ 可用（但需登录） |

**实测结论（2026-09-06）**：
- 个人站的 weread RSS 路由是 `/api/wechat/rss/<mp_id>?ext=xml`。
- 从本机裸请求该地址 → **`401 未登录`**。个人站 RSS 锁着它自己的账号登录，本机无法直接消费。
- 所以：**扫码授权的是个人站，本地这套从没被指过去，两套之间没有任何连接**。这就是"为什么扫了码本地还是 0 新增"的答案。

已知的一个可用 mp_id：`MP_WXS_2398602260`（腾讯技术工程）。

---

## 11. 未决选择：Kyle 尚未拍板的三条路

接手后**先问 Kyle 选哪条**，不要自作主张：

**① 本地起 we-mp-rss（最正统，匹配现有 docker-compose）**
```
启动 Docker Desktop → docker info 确认就绪
docker compose -f AI/.tools/docker-compose.wechat.yml up -d
浏览器开 http://localhost:8001 → 扫码登录 → 订阅公众号 → 拿 RSS 地址
填回 subscriptions.json 各账号 rss 字段 → detector.enabled=true
```
优点：本地独立、实时、不依赖个人站。代价：本机需常驻 Docker；we-mp-rss 要**再扫一次码**（与个人站那次不同）。

**② 让本地 sync.py 消费个人站 weread RSS（复用已扫的授权）**
需给 sync.py 加登录态（token/cookie）去请求 `/api/wechat/rss/<mp_id>`。
缺点：① 要存凭证；② weread 只采集**在微信读书里读过**的文章，未必是完整实时流。

**③ 维持 inbox 手动投喂（当前实际在用）**
Kyle 看到好文把链接丢进 `inbox.txt`，07:30 自动归档。零运维、已验证可用（2026-09-06 归档的阿里技术那篇就是这么进来的）。

---

## 12. 历史文档与记忆位置

| 位置 | 内容 |
|---|---|
| `公众号订阅自动化方案.md` | 方案审查与落地记录（设计论证、实测数据、风险表） |
| `.workbuddy/memory/MEMORY.md` | 项目长期记忆（模块定位、归档纪律、部署踩坑） |
| `.workbuddy/memory/2026-09-06.md` | 当日工作日志 ⚠️ **该文件内容对模型屏蔽**（Kyle 明确拒绝共享其中的敏感值），读取会返回屏蔽提示，不要反复尝试或试图绕过；需要追加时请用 Python 以 UTF-8 追加写入 |
| `.workbuddy/memory/automations/512ae303-.../memory.md` | 定时任务逐轮执行记录（含无更新轮次） |

---

## 13. 接手后建议的第一步

1. `sync.py --dry-run` 确认管线在当前机器上可跑通（零副作用）。
2. 读 `archive_state.json` 了解已归档 16 篇，避免重复劳动。
3. **问 Kyle 第 11 节三条路选哪条**，再动配置。
4. 若选 ①：务必先补 `discover_from_rss` 的日期过滤（坑 2），否则首轮可能灌进大量历史文章。

---

## 14. 2026-09-06 晚更新：全自动通道已接通（本文档第 8/10/11 节状态已过时）

Kyle 需求定版：**网站自动检测订阅号更新（每日 07:30 即可），保留原文链接点击跳转原文，不手动投喂**。第 11 节三条路的最终答案是「②+③ 混合」——自动检测走服务器上的 we-mp-rss（个人站体系内），本地 Knowledge 管线维持 inbox 手动归档轨道。当日晚间已完成全部接线并端到端验证：

- **授权**：微信读书 Cookie 已重新投喂 we-mp-rss（该容器只绑 docker 内网 `172.17.0.1:8001`，管理 API 前缀 `/api/v1/wx/`，默认凭据 admin/admin@123）。
- **订阅**：腾讯技术工程 `MP_WXS_2398602260`、阿里技术 `MP_WXS_3885737868` 两个 feed 均在采集。
- **定时**：we-mp-rss 消息任务「公众号每小时采集」cron `17 * * * *`（⚠️ 无消息任务=永不自动追更）；网站定时同步维持每日 07:30。
- **网站**：账号「阿里技术」已挂 feed_url；`WECHAT_MIN_IMAGES=0` 已追加进服务器 `.env`（纯文字文章也入库）；手动 sync 已验证入库文章且 source_url 正确。
- **踩坑**：weread 返回的文章 token 含 `~` 时微信真实链接是 `_`，原样保留会被判「参数错误」——已在 we-mp-rss 容器内打补丁（详见 MEMORY.md「个人站公众号自动检测全面接通」一节，补丁脚本 `Knowledge/.workbuddy/patch_werss.py`，容器重建后需重打）。
- **待办安全项**：网站密码（默认 studybuddy2026）、we-mp-rss admin 密码（默认 admin@123）、服务器 SSH 密码——三者都是弱口令，建议尽快更换。
- **Cookie 再过期的运维手册**见 MEMORY.md 同节（征兆：列表停止更新）。
- **渲染/显示修复（同晚）**：commit `f45da16` 已部署——typography 排版、软断行还原、长 URL 溢出、发布时间（feed pubDate 兜底）、账号归属（订阅号优先）等 6 处，明细见 MEMORY.md「peronalsite 显示/渲染修复」一节。peronalsite 本地副本现于 `Desktop/peronalsite`。
