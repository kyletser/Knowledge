# 公众号订阅自动化 · 方案审查与落地记录

> 审查日期：2026-09-05　结论：**核心管线已全部落地并通过实测**，剩 2 件事必须你本人操作。

---

## 状态总览

| 项目 | 状态 |
|---|---|
| 归档管线（抓取 + 图片本地化 + 推广剥离） | ✅ 已跑通（复用现成脚本） |
| 归档铁律：软断行 ≤200 字符 | ✅ 已补齐并实测达标 |
| 归档铁律：正文无图不保存 | ✅ 已补齐并实测达标 |
| 归档铁律：限流不重试 | ✅ 已实现 |
| 去重状态库 | ✅ 已实现并实测 |
| 每日 07:30 定时任务 | ✅ 已创建（ACTIVE） |
| 阅读索引（md + HTML） | ✅ 已实现 |
| **公众号订阅清单** | ⏳ **等你给** |
| **Docker 检测服务（全自动）** | ⏳ **等你启动 Docker Desktop + 扫码** |

---

## 一、审查发现：原方案有 3 处假设被实测推翻

我用一篇真实文章（腾讯程序员《AI Coding的下一站》）跑通全流程后逐条比对，发现原方案写的和脚本实际行为对不上：

| 原方案写的 | 实测结果 | 处置 |
|---|---|---|
| 软断行 ≤200 字符 | **脚本没做**。实测最长行 **308 字符**，5 行超标 | 在 `sync.py` 补后处理 |
| 零图不保存（保存门槛） | **脚本没做**，零图文章照存不误 | 在 `sync.py` 补门槛 + 清理已下载产物 |
| 格式只有「公众号 + 链接」 | 实际还会输出 `> 发布时间`（有该字段时），并额外存一份 `original.html` 保真备份 | 订正认知，`original.html` 是有益的，保留 |

**软断行还有一个隐藏坑**：初版实现把 `>` 开头的引用行整体跳过，结果剩下 1 行 282 字符仍超标（微信的长导语往往就是超长引用行）。已修正为**保留 `>` 前缀、只断行内容**。

修复后实测：最长行 **308 → 200**，超标行数 **5 → 0**，图片数 **10 → 10（零损失）**。

---

## 二、已落地的组件

```
AI/.tools/
├── wechat_archive.py              # 原有，未改动（抓取/本地化/剥离推广）
├── sync.py                        # 新增，一体化管线（检测→去重→归档→索引）
├── subscriptions.json             # 新增，订阅配置（公众号清单 + 检测开关）
├── archive_state.json             # 新增，去重状态库
├── inbox.txt                      # 新增，手动补链接通道
├── docker-compose.wechat.yml      # 新增，we-mp-rss 编排（待 Docker 就绪后用）
└── requirements-wechat.txt        # 原有，依赖清单
```

**`sync.py` 补齐的三条铁律**（wechat_archive.py 原本没有）：

1. **软断行 ≤200**——只断行正文段落，跳过代码块、图片、标题、表格、列表；引用行保留 `>` 前缀断行内容。
2. **保存门槛**——正文图片数低于 `min_images`（默认 1）则**放弃保存并删除已下载产物**（md + assets + original.html），不留垃圾。
3. **限流不重试**——命中「访问过于频繁/环境异常/验证码」即止，链接保留在 inbox 等下一轮，不反复请求。

**命令**
```bash
python sync.py                # 完整跑一轮
python sync.py --dry-run      # 只列出会归档什么
python sync.py --index-only   # 只重建阅读索引
python sync.py --inbox-only   # 跳过 RSS，只消费 inbox
```

---

## 三、实测验证结果

| 测试 | 结果 |
|---|---|
| 软断行 | 最长行 308 → 200，超标 5 → 0，图片 10 → 10 无损 ✅ |
| 正常归档 | 状态 ok，10 图，assets + original.html 齐全，最长行 200 ✅ |
| 零图门槛 | 抬高门槛强制触发 → 状态 zero_image，md 已删、assets 已清空 ✅ |
| 首次同步 | 归档 1 篇，生成索引，inbox 自动清空已消费链接 ✅ |
| 二次同步 | 正确识别重复，0 新增 ✅ |

> 补充确认：当前环境**能正常访问 mp.weixin.qq.com**，定时任务的网络前提是成立的。

---

## 四、仍需你完成的 2 件事

### 1. 给我公众号清单（唯独这个我猜不出来）
打开 `AI/.tools/subscriptions.json`，在 `accounts` 里填入你要追踪的号：

```json
"accounts": [
  { "name": "腾讯程序员", "enabled": true, "rss": "" }
]
```

建议 5~10 个。填了 `name` 即使没有 RSS 也能用——归档时会标记来源，走 inbox 手动通道。

### 2. 想要全自动，就得启动 Docker + 扫码（可跳过）
当前状态：**docker 命令有（29.7.2），但守护进程没运行**，所以 we-mp-rss 起不来。

```bash
# ① 先启动 Docker Desktop，确认守护进程就绪
docker info

# ② 起检测服务
docker compose -f AI/.tools/docker-compose.wechat.yml up -d

# ③ 浏览器打开 http://localhost:8001 扫码登录，订阅公众号，
#    把每个号的 RSS 地址填回 subscriptions.json 的 rss 字段，
#    再把 detector.enabled 改成 true
```

**不做这步也能用**——走 inbox 通道：你把链接丢进 `AI/.tools/inbox.txt`，每天 07:30 自动归档。这正是我设计双轨的原因（微信登录态失效是常态，inbox 是永久兜底）。

---

## 五、日常怎么用

- **临时补一篇**：链接丢进 `inbox.txt` → 等 07:30 自动跑，或手动 `python sync.py`
- **看今天收了什么**：打开 `AI/阅读.html`（带搜索框和按公众号筛选）
- **改配置**：`subscriptions.json` 里调 `min_images`、`soft_wrap_width`、检测范围
- **清掉误判**：某篇被判「零图放弃」不该丢的话，删掉 `archive_state.json` 里对应条目再跑

---

## 六、已知问题与风险

| 风险 | 影响 | 兜底 |
|---|---|---|
| **Docker 守护进程未运行** | 全自动检测通道不可用 | inbox 通道完全可用，功能不残缺 |
| 微信登录态失效 | RSS 检测断流 | inbox 永久兜底 |
| 单链接被风控 | 个别文章抓不到 | 单次请求不重试，留待下轮 |
| 07:30 电脑没开机 | 当天任务跳过 | 状态库保证不漏不重，下次补上 |
| **图片字节冗余** | 部分公众号的通用装饰图/动图会在该号每篇文章里各存一份（此前实测 `AI/` 约 69MB 属此类） | 属"按文章隔离资源"的结构性代价；要消除需改共享图池 + 批量改链接，性价比低，暂不动 |
