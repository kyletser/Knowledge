# 2026-08-06 工作日志

- 公众号存档：单次抓取《从 Agent Flow 到 AI Native：为什么通用 Agent 是"饮鸩止渴"》（mp.weixin.qq.com/s/_ctmgcnt-G46bmrFmR2vZg），对齐现有 12 篇格式。
- 对齐修复（仅改生成的 md，未动 .tools/wechat_archive.py）：
  - 8 个主章节（01–08）与"第一，…"提为 `###` 小节（与原文已有的 `### 第二` 一致）
  - 导语 block 纳入"（本文阅读时间…）"行
  - 附录两条纯文本链接格式化为可点击列表
  - 剥离文末 2 个 gif + "欢迎留言"互动句（保留作者免责声明）
  - 超长论述段软断行（在标点处切，GFM 仍连读），最长 189 字符
- 校验：无 >200 超长行、图片 4 张全本地化、无远程/推广词残留、`###` 小节共 11 个。
- 注意：抓取后必须先 Read 源 md 再编辑（首次编辑因"0X 独立行与标题间有空行"判断失败，已修正为跳过空行取标题）。

- 升级 `wechat-to-md` 技能为自包含通用版（用户要求"通用 skill，所有智能体通过链接直接抓，含一条或多条"）：
  - 把抓取脚本从工作区 `.tools/wechat_archive.py` 复制并增强进技能 `scripts/wechat_archive.py`（不再依赖工作区文件）。
  - 增强脚本：`polish_markdown` 固化章节提升（裸 `01`/`一`/`①`+标题 → `### 01 标题`；`第一，` 段首 → `### 第一，`）；新增 `soft_wrap` 长行软断行（≤200）；新增 `--html <file>` 本地 HTML 兜底入口；`urls` 本就支持 `nargs="+"`（多链接）。
  - 重写 SKILL.md：改用技能自带脚本路径、固化标准输出格式、保留限流纪律（单次抓取）+ WebFetch 兜底 + 本地 HTML 兜底；frontmatter 加 `agent_created: true`。
  - 已用合成输入做无网络单元测试（章节提升/软断行全过），并用 skill-creator `package_skill.py` 校验打包为 `D:/FILE/个人/AI/wechat-to-md.zip`（校验通过）。技能装于用户级目录，跨工作区可用。

- 端到端验证新技能：用 `wechat-to-md` 跑 `mp.weixin.qq.com/s/yFHRoAi6fe2dduXXlM8Tzw`（得物技术《实战从零开始构建一个Coding Agent：Violin》）。
  - 脚本单次抓取被微信限流（`original.html`=459B 空壳，md 仅 251 字）→ 限流检测正确触发。
  - 按技能兜底走 WebFetch 独立通道拿到全文（17971 字），写 `_violin_raw.txt` 后用 `_rebuild.py` 结构化重建：目录包入 `> **导语**：`、章节序号+标题合并为 `### 一、背景`、独立加粗小标题提为 `###`、25 个代码块原样保留、首图 gif 内联、缺失配图附文末图集说明；软断行至 ≤200 字符。
  - 成品：`实战从零开始构建一个Coding Agent：Violin ｜得物技术.md`（40 个 ### 小节，0 超长行）。临时 `_violin_raw.txt`/`_rebuild.py` 已清理。
  - 结论：技能「脚本抓取 → 限流检测 → WebFetch 兜底重建」全链路跑通，通用化成功。限流冷却后重跑脚本可补回正文内联配图。

- 用户纠正：限流兜底重建的 Violin 文末尾我加了「## 本文配图」占位块（仅重复封面图+说明），用户不满：「以后没有图片就别生成了」。已修正：
  - 删掉该文末尾冗余的 `## 本文配图` 区块（封面图已在 `---` 后内联，无需重复）。
  - 在 `wechat-to-md` SKILL.md 兜底流程第 6 条加铁律：**无实质配图（限流致正文图缺失，仅封面或零图）则不生成任何图片区块**，不要占位说明。

- 用户再次升级规则（@09:47）：「没有文章内部配图的文章就不要保存。」
  - 含义落地：落盘前提是文章含「内部配图」（正文图，封面不计入）。限流致内部图全缺失、或纯文本零图文章，都**不保存**。
  - 在 `wechat-to-md` SKILL.md 限流铁律后新增「🚫 保存门槛：必须有文章内部配图」：限流残缺/零图时删掉已生成的 md 与 assets 子目录，等冷却后重跑脚本拿完整内联图版再存；纯文本零图直接跳过。
  - 按新规则删除了上一轮存错的 Violin 文（`D:/FILE/个人/AI/实战从零开始构建一个Coding Agent：Violin ｜得物技术.md` 及其 assets 子目录已删），因其限流后无内部配图。
  - 注：限流冷却后若用户要完整版，需重跑脚本（单次抓取）拿内部图再存；当前仍限流，暂不复抓。

- 公众号存档（单次抓取，符合「有内部配图才保存」规则）：`mp.weixin.qq.com/s/gZKWRqznB8sNBFf69fBIvw` → 《Agent评测漫谈 —— 由浅入深讲解Agent评测.md》。
  - 抓取正常（`original.html`=144KB，无限流），正文内部配图 19 张（封面1+正文19），assets 22 文件，满足保存门槛 → 保留。
  - 格式：13 个 `###` 小节、元信息/导语齐全、0 超长行、无远程残留、无推广词。
  - 按全库惯例剥离文末「欢迎留言」互动句与 `---------- END ----------` 标记，保持与其他存档一致。
