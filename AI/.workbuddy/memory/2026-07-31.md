# 2026-07-31

## 修复「前端 Skill 驱动…」md 中被压扁的代码块
- 用户反馈目录树（`an-frontend-skill/`）没有像原文那样「复制出来」。根因：微信公众号文章里这类「树形/列表式代码块」实际是用若干 `<p>`/`<span>` 拼的，脚本用 markdownify 转换时把 `<p>` 边界 flatten 了，导致整段被压成一两行。
- 修复范围（整篇文章扫描后全部重排）：
  1. `an-frontend-skill/` 目录树（用户指出的）
  2. `## 触发场景` 列表块
  3. `## 硬性约束（违反将被拒绝）` 8 条规则
  4. `### 设计语言（必读）` + Design Token 表 + 国际化 + 调用顺序
  5. i18n 流水线 6 步脚本块
- 校验：所有代码块内再无超过 200 字符的超长行，说明已正常分行。
- 文件：`D:/FILE/个人/AI/前端 Skill 驱动的团队 AI Coding 实践：从个人到整体提效.md`。
