# 2026-07-30

## 公众号转 md 工具（.tools/wechat_archive.py）修复与本次抓取

- **strip_promo 重写**：原实现从末尾倒序遍历，遇到尾部隐藏 `<p style="display:none">`/空壳 section 就 `break`，导致前面的文末推广块（"扫码添加小助手微信"+二维码图）漏删。新版改为：仅在文末最多 10 个块里查找最后一个命中 `PROMO_RE` 的块，然后从该块删到末尾（推广永远在文末，连带尾部空壳/隐藏节点一起清）。只认文字推广词，避免误删正文图（之前曾把 Marvis 尾部 2 张无文字配图当二维码误删）。
- **add_daoyu_label 增强**：原逻辑只在 `---` 后第一个 `> ` 块（导语引文）时生效；新增：开头为 `**目录**`/「一、二、」目录列表时也包进 `> **导语**：`，并对已存在该标签的文章幂等跳过。
- **本次抓取（得物技术文）遭遇微信限流**：URL `https://mp.weixin.qq.com/s/B29yrjptZiV4d_6cSskjmg`。首次脚本抓取成功（9829 字符、24 图），但随后短时间内多次抓取触发本机 IP 临时封禁，后续脚本抓取返回 `#js_content` 几乎为空的壳页面（original.html 仅 441 字节）。
- **兜底重建**：用 WebFetch（独立抓取通道，未被限流）拿回完整正文，手工整理成对齐格式 md：标题+`> 公众号：行亦`+`> 原文链接：`+本地首图+`> **导语**：`目录+`###` 章节（主章节「一、…五、」合并、子标题独立）+剥离「往期回顾」；其余 23 张本地图以文末图集附上（抓取受限未能内联）。
- ⚠️ **重要**：当前 md 是 WebFetch 兜底版（图在文末图集，非内联）。限流解除后重跑脚本 `python .tools/wechat_archive.py "<url>" -o "D:/FILE/个人/AI" --overwrite` 可得带内联图的完美版。**在限流未解除前不要重跑脚本**，否则会把这份兜底版覆盖成残缺版。

## 沉淀为可复用技能
- 已创建用户级技能 `wechat-to-md`（`C:/Users/Administrator/.workbuddy/skills/wechat-to-md/SKILL.md`），固化本次全流程：脚本主路径 + 微信限流检测（original.html 体积 / md 字符数）+ WebFetch 兜底重建启发式（章节序号合并、普通短语子标题独立、剥离往期回顾、其余图作文末图集）+ 校验清单 + 铁律"限流未解除前不重跑脚本"。下次给链接直接按技能走，避免重复试错。

## 14:46 用户要"完整版"（内联图）— 仍被限流 + 踩了 overwrite 坑
- 用户要带内联图的脚本完美版。14:46 重跑脚本 + 等 4 分钟再跑，均失败：`original.html` 仍为 441 字节 → **本机 IP 封禁是持续性的（4 分钟不够，可能 1 小时+ 或等 IP 轮换）**。
- **overwrite 坑**：限流期间多次 `--overwrite` 把上一轮那份完整兜底版覆盖成了 233 字符空壳。已用 WebFetch 重新拿全文、重建恢复（8865 字符、30×###、导语、往期回顾剥离、24 图：首图内联+23 图集）。当前交付=完整内容版。
- **教训/待办**：脚本应加"限流自检"——若 `#js_content` 文本过短（如 original.html < 2KB）则拒绝写入/覆盖并报错，避免 overwrite 把好版本冲掉。下次可做此加固。
- **给用户的两条拿内联图路径**：① 等更长冷却后重跑脚本；② 用户在自家浏览器（未封 IP）保存文章完整 HTML 给我，我处理本地 HTML（需给脚本加"读本地 HTML 文件"入口）。方案②可彻底绕过 IP 封禁。

## 17:43 新链接跑通（IP 封禁已解除）
- 用户给新链接 `https://mp.weixin.qq.com/s/kz_7d66dhmPhOUlcMh4MzA`。按"少抓几次"单次抓取（不带 --overwrite，防误伤）：**一次性成功**，确认本机 IP 对 mp.weixin.qq.com 的限流已解除（换篇文章即正常，说明此前是短时频抓触发的短时封禁）。
- 产物：`AI Coding的下一站，不是更会写代码，而是更懂团队.md`（12224 字符，original.html=164761 字节）。格式对齐：h1×1、`> **导语**：`×1、`###`×9、本地图×10、远程图=0。文末推广按脚本 strip_promo 自动剥离。
- 推论：wechat_archive.py 管线 + 限流规避策略（单次抓取 + 本地备份 + 限流即恢复不重试）已验证可行。

## 17:47 重试得物技术文（同 URL）仍被限流 — 发现封禁更可能是「按 URL 命中」
- 用户再次给 `https://mp.weixin.qq.com/s/B29yrjptZiV4d_6cSskjmg`（即之前被限流那篇）。17:43 同域名另一篇已跑通，但本篇单次 `--overwrite` 仍返回 `original.html=441字节 / md=233字符`。
- 推断：微信限流**按具体文章 URL 命中**（这篇被高频打过、被单独标记），并非整 IP 全封——所以新 URL 正常、旧 URL 仍壳。
- 已本地备份（20267 字节）→ 抓取失败 → 恢复备份、未重试。当前磁盘 = WebFetch 兜底完整版（全文+24 图集），完好。
- 结论：该篇内联图完美版用脚本在本机已拿不到，只能走「用户存浏览器完整 HTML → 我处理本地文件」通道（需给脚本加 `--html <file>` 入口）。

## 10:25 新链接跑通（全新 URL 未被按-URL 标记）
- 用户给新链接 `https://mp.weixin.qq.com/s/cMzngHjtMacdXNnD-Y6Ieg`（同域、但此前未高频打过，不在按-URL 封禁名单）。按"少抓几次"单次抓取（不带 --overwrite，防误伤）：**一次性成功**，进一步佐证此前 B29y 那篇是被单独 URL 标记、而非整 IP 封。
- 产物：`前端 Skill 驱动的团队 AI Coding 实践：从个人到整体提效.md`（15531 字符，original.html=302168 字节）。格式对齐：h1×1、`> **导语**：`×1、`###`×20、本地图×9、远程图=0。文末推广按脚本 strip_promo 自动剥离。
- 推论：全新文章的脚本抓取在本机完全可行，无需本地 HTML 兜底通道。

## 16:00 用户说"重新" — 单次抓取确认封禁仍在 + 已加固防 overwrite 误伤
- 按"少抓几次"规则，先本地备份当前好版本（20267 字节），再**只发 1 次**脚本请求。结果 `original.html` 仍为 441 字节、md 233 字符 → **本机 IP 封禁持续未解除**（距首抓已数小时）。
- 立即用本地备份恢复好版本、未重试。当前磁盘上的得物技术 md = 完整内容版（WebFetch 兜底，全文+24 图集），完好无损。
- 结论：内联图完美版仍需等 IP 解封，或走"用户存浏览器 HTML → 我处理本地文件"通道。下次若要做本地 HTML 入口，可在 `wechat_archive.py` 加 `--html <file>` 参数。
