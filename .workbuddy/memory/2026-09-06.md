# 2026-09-06

## 公众号订阅全链路打通（we-mp-rss → 研途助手）✅

凌晨完成最后一步。全链路状态：

1. **we-mp-rss 微信读书授权**：扫码 5 次全失败后改 Cookie 方式，一次成功（wr_vid=300739622）。`POST /api/v1/wx/weread/cookie` 保存 → `/weread/test` 连接成功（书架 35 本）。
2. **公众号身份解析**：`POST /api/v1/wx/mps/by_article?url=...`（query 参数）解析用户提供的文章链接 → `mp_name="腾讯技术工程"`、`mp_id="MP_WXS_2398602260"`、`biz="MjM5ODYwMjI2MA=="`。
3. **采集**：`POST /api/v1/wx/weread/collect {"mp_id":"MP_WXS_2398602260","gather_content":true}` → 采集 1 篇（Cordis 文章全文 HTML）。
4. **RSS 验证**：`GET /rss/MP_WXS_2398602260?ext=xml` 返回完整 RSS 2.0；studybuddy 容器内访问 172.17.0.1:8001 同 URL → 200。
5. **研途助手接线**：
   - 容器 openapi.json 探测确认 schema：`POST /api/wechat/accounts` 仅必填 `name`，可选 `feed_id/feed_url/enabled/note`。
   - 登录：`POST /api/auth/login {"password": SITE_PASSWORD}`（容器 env 已设 14 字符自定义密码）。
   - 建账号成功（id=dcad6d7f88da，feed_url=http://172.17.0.1:8001/rss/MP_WXS_2398602260?ext=xml）。
   - `POST /api/wechat/sync` → added 1 篇，0 错误 0 限流，耗时 1.4s。
   - 文章详情验证：markdown 全文 35,651 字符、image_count=16、word_count=27804、source_url 正确、格式对齐 Knowledge 归档规范。
6. **定时任务**：next_run_at=2026-09-06T07:30:00+08:00，明早首跑。

### 踩坑记录
- `POST /api/v1/wx/mps` 显式建 feed 报 50001「添加公众号失败」且 `GET /mps` 一直为空——但 collect/RSS 完全不依赖该列表，不影响链路（we-mp-rss 此分支直接以文章库为准）。
- `GET /api/wechat/inbox` 只支持 POST（手动投喂 urls），读列表要用 `GET /api/wechat/articles`；文章全文只在详情接口 `GET /api/wechat/articles/{id}` 的 `markdown` 字段，列表接口不返回。
- 文章 publish_time 为空（RSS item 无 pubDate），小瑕疵不影响阅读。
- weread 通道 `gather_model="web"` 一次只采最新一篇，历史文章不回填，只有新文章进来。
- 服务器时区注意：容器 created_at 是 UTC（16:26 UTC = 北京 00:26），RSS lastBuildDate 是 +08:00。

### 遗留提醒（已告知用户）
- 服务器 root 密码 LXPzxy2002115 需修改；we-mp-rss admin 默认密码 admin@123 需修改。
- 微信读书 Cookie 会过期：出现 -2012/-2041 错误时需重新 F12 复制 weread.qq.com 的 Cookie，再 `POST /api/v1/wx/weread/cookie`。

## 公众号订阅同步（10:12）
- 加订阅号「阿里技术」到 AI/.tools/subscriptions.json（rss 留空，仅走 inbox 手动通道）。
- 投递并归档 1 篇：AI Agent 应用精细化评测：评测体系设计与工程实践（mp 链接 5Tvv8g20CybjbT0a7iUfHw，23 图）。
- 修正 archive_state.json 该条 account 由 inbox→阿里技术，--index-only 重建索引。存量 16 篇。

## 公众号订阅项目交接文档（15:40）
- 产出 公众号订阅项目交接文档.md（仓库根），供其他 AI 接手：文件地图/管线/配置/三条铁律/定时任务/当前状态/五坑/两套系统关系/三条未决路径。
- 核实并订正：subscriptions.json 的 detector.base_url 与 settings.检测范围_天 均为死配置（代码从不读取）；RSS 检测无日期过滤，开启后会全量灌历史文章。
- 实测：个人站 weread RSS 路由 /api/wechat/rss/<mp_id>?ext=xml 返回 401 未登录；docker 守护进程仍未运行。
- 现状：归档 16 篇，腾讯程序员/阿里技术 rss 均空，detector 关，自动检测未接通。

## learn-claude-code 多语言 README 清理（15:52）
- Kyle 指示「只保留中文的」→ 删除非中文 README 42 个 + 配套 .en/.ja 插图 58 个，共 100 个，全部送回收站，0 失败。
- 保留：根 README-zh.md + s01–s20 各 1 个中文 README.md（21 个）。
- **关键教训**：字符比例启发式会把 s04_hooks/README.md（1248 汉字但代码块多）误判为 en，差点删掉全章唯一中文版。改用 H1 标题行判定（假名→ja，有汉字→zh）后正确。
- 未动：web/public/course-assets（87 个，网站发布副本，删了站点会挂）；web/README.md（Next.js 脚手架英文说明，无中文对应版，待 Kyle 定夺）。
- 删除清单留存于 .workbuddy/scan_del.txt，回收站可还原。

## AI 图片去重 + 共享图池（16:00）
- 删除 41 个重复装饰图 → AI/ 从 138.2 MB 降到 69.0 MB，净省 69.2 MB。重复图归一到 AI/assets/_shared/<md5><ext>（6 个），md 链接改指共享池。
- **踩坑（重要）**：解析 md 图片链接的正则 `[^)\s>]+` 排除了空格，导致 13 篇标题含空格的文章（如『AI Coding的下一站』）完全没被解析，改链接时漏掉，删原文件后造成 22 条断链。正确写法：`!\[([^\]]*)\]\(\s*<?([^)>]+?)>?\s*\)`（允许路径含空格）。
- 修复手段：① 改动前的 md 备份（.workbuddy/md_backup）做『备份 vs 已改写』对照，还原 原文件名→共享文件名 映射（3 条）；② 剩余 2 条用回收站 $I/$R 解析原路径 + 算 MD5 确证（共享文件本就以 md5 命名，可对上）。
- 终检：229 条图片引用全部可解析，断链 0。4 个 `![]()` 空占位符是微信转换遗留、改动前既有，无害。
- 另发现：16 个 original.html 的 217 处本地图片引用**本来 100% 断链**（html 在 assets/<标题>/ 里却按 AI/ 根写相对路径）。
- 遗留：assets 中仍有 153 个（约 44.7 MB）孤立图无任何 md 引用，未删，待 Kyle 定夺。

## 清理零引用孤立图（16:33）
- **更正先前误判**：之前报「153 个孤立图 / 44.7 MB」是用坏正则算的。正确正则重算后，md 实际可解析引用 205 个文件，真正孤立仅 6 个 / 0.75 MB。
- 判定方法：先取 md 中可解析引用集合，再对候选文件做**全仓库 632 个文本文件**的文件名扫描；仅出现在 original.html（其链接本就 100% 断链）或完全无人提及者才删。闸门拦截 0 个。
- 删除 6 个（0.75 MB），全部送回收站。终检 229 条引用零断链。AI/ 现 68.2 MB（原 138.2 MB）。
